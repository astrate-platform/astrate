# Candidate issues — 2026-08-26

Distilled from `survey-2026-08-26.md`. The same four candidates as yesterday — triage has
not filed any of them yet (open-issue list re-read today: only #91/#87/#78/#68/#67 open) —
carried forward with every citation re-measured today against raw files at `9c4d411f`;
upstream citations re-fetched at master `ac04ca95`. Candidate 1 is materially stronger than
yesterday: upstream's own code now contradicts the sentence Astrate's ACL comment leans on.
No new candidates: the delta's remaining news (#67 fase 4a, encrypted-endpoints telemetry)
already has a home or belongs behind the v1.4.0 release gate.

---

### keyagreement-acl-residue
Title: Let control/keyAgreement reach the engine's unknown-control tolerance (or cite why deny wins)
Scope: mule-line
Body:
#51 closed as completed on its minimum obligation ("tolerate unknown control paths
gracefully"), and the engine honours it — unhandled control subpaths get Warn + Ack
(`internal/engine/control.go` default arm). But `<realm>/<device>/control/keyAgreement`
never reaches that code: the broker ACL allowlist (`internal/broker/aclhook.go:62`) omits it,
so a spec-following device gets a **broker denial** where upstream's doc promises publishes
there are "accepted and acknowledged by Astarte, but no response is sent". The deny comment
(`aclhook.go:65-68`) quotes only that section's other sentence ("reserved and routed
correctly, but not yet implemented") and concludes deny "matches upstream's own status".
**Stronger today**: upstream master `ac04ca95` instruments a working key-agreement state
machine on `control/keyAgreement/4` (`control_handler.ex` succeeded/failed arms, DEK cache,
encrypted-message handling in `data_handler.ex`) — while the protocol doc still carries both
old sentences verbatim (lines 174/176, re-read in the clone). The comment is now stale
against the code it cites *and* half-stale against the doc. With #51 closed nothing owns this
residue. Fix: one ACL branch letting `control/keyAgreement` fall through to the engine's
tolerant default arm (flip the `"pub control keyAgreement"` test row at
`aclhook_test.go:53` from deny to accept), plus rewriting the comment to cite both sentences
and note the handshake now exists upstream. Full DEK/encrypted-endpoint support stays parked
behind v1.4.0; the mapping-field side remains #67.

### stale-auto-queue-lines
Title: Tick the two `[auto]` todo lines already covered by closed issues #47/#48
Scope: mule-line
Body:
`.mule/todo.md` on main still queues `purge-properties-compression-capability [auto]` and
`empty-introspection-verification [auto]`, but both are implemented and their tracking issues
are closed: capabilities handling defines and validates zlib|plaintext in
`internal/engine/capabilities.go:14-20` (re-grepped today at `9c4d411f`; #47), and empty
introspection is tolerated in `internal/engine/introspection.go:96-101` (#48). Each open
line risks costing a refill tick from the daily budget. One commit ticking/annotating both
lines with their issue numbers; no code. While touching the file:
`compat-note-v1.3.2 [auto]` calls v1.3.2 "latest stable" — it is v1.3.3 since 2026-08-07
(re-confirmed via tags API today), whose single changelog entry (the resend-failure
error-name split) is exactly what that compat-note run should cover when it executes.

### errorname-table-alignment
Title: Fix unset_not_allowed mapping and add decryption_error/malformed_capabilities_message to the errorname table
Scope: mule-line
Body:
`internal/engine/triggers/errorname.go` still maps `unset_not_allowed` into the
`unexpected_value_type` bucket (`errorname.go:62`, re-read today) instead of its own name,
and still lacks `decryption_error` and `malformed_capabilities_message` entirely (grep).
These were parked as "#57-confirm" rows, but #57 closed 2026-08-24 without touching this
file, leaving the corrections homeless. The table edit itself is mechanical; the one
judgment call — confirming each correct target string against what upstream actually emits
— got sharper today: upstream's new encrypted-endpoints code uses `decryption_error` and
`key_agreement_error` as internal failure reasons (DUP telemetry tags + ask-clean-session
disconnects at master `ac04ca95`), so verify per name whether upstream surfaces it as a
device-visible MQTT error_name or only internally before adding the row. Add test rows
mirroring the existing mapping-table tests.

### resend-failure-device-error
Title: Emit device_error on properties-resend failures; add upstream v1.3.3 load-vs-send split
Scope: mule-spec
Body:
`resendServerProperties` (`internal/engine/control.go:144`) logs-and-skips every failure mode
silently — uninstalled interface (`:155`), unmatched path (`:161`), non-rehydratable value
(`:167`), non-encodable value (`:173`; all four re-measured today at `9c4d411f` — yesterday's
`:153/159/165/171` were off by two, file untouched by the delta) — so a device whose
emptyCache resync partially fails sees nothing; Astrate maps
`resend_interface_properties_failed` / `empty_cache_error` but never emits either. Upstream
≥v1.3.3 additionally splits load-failure from send-failure during resend
(`interface_loading_failed` vs `resend_interface_properties_failed`); Astrate emits neither.
Behavioural addition (new device-visible events), hence mule-spec not mule-line: pick per-
failure-mode names against the measured upstream split, decide whether store errors also
surface or keep propagating to the caller's retry loop, and cover with an integration test.

---

## Retirement/re-home notes for triage

- **All four candidates above are second-day carries** (filed nowhere as of today). If they
  are not going to be filed, say so here so the survey stops carrying them; carrying a third
  day without a decision wastes the re-verification each run pays for.
- **Malformed-capabilities disconnect semantics**: behaviour standing (reject-and-keep vs
  upstream force-disconnect), still no open issue, and the proposed fresh for-giulio
  design-decision entry still has not been written to main's `.mule/for-giulio.md`
  (re-checked today). New context for whoever writes it: upstream's encrypted-message
  handling also force-disconnects on failure (`ask_clean_session: true`) with an inline TODO
  about softening — upstream's own instinct may converge with Astrate's reject-and-keep.
- **#67 fase 4a landed on astrate main** (`9c4d411f`): parse/gate only, runtime enforcement
  of `required` explicitly deferred within open #67. No action needed from triage; recorded
  so no candidate invents a duplicate home.
- **Encrypted endpoints watch item (not a candidate)**: upstream master now has live DEK
  caching + key agreement + encrypted-message handling, shipping in 1.4.0 (rc-only). When
  that releases, Astrate's parsed-but-inert `Encrypted` flag becomes a full parity work area;
  #67 owns the attribute slice, the wire side has no owner yet.
- **Group-scoped triggers / BSON-null individual rejection**: unchanged, parked/carried
  (`match.go:393-394,465-466`, `payload_test.go:363,499` re-read today); deliberately kept
  out of the candidate list under the cap.
