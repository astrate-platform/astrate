# Dates triaged by mule-triage — one per line, oldest first

2026-07-27 — the calibration run, triaged by hand before the mule-triage skill existed.
Filed as GitHub issues #12-#19 (5 labelled `mule`, 3 deliberately not — see their bodies)
and 4 entries in `.mule/for-giulio.md` on `main`.

2026-07-28 .. 2026-09-04 — backlog triage of the 17 surveys that had accumulated untriaged
since the calibration run. Almost the whole candidate set had already been filed and closed
independently as issues #12-#91 during the v2.0 milestone work, so the live residue was small.
Filed: #92 (keyAgreement — the parking condition from #51 has fired; scope decision, not a mule
task, also recorded in .mule/for-giulio.md) and #93 (mule: the broker ACL comment quotes an
upstream doc sentence deleted in d084308). Both claims re-verified against upstream master and
current main rather than taken from the surveys. Deliberately not filed: the BSON-null
individual-value rejection (standing, still homeless, held back on cap discipline) and the
group-scoped trigger decision (already deferred in for-giulio, tied to #17). Noted but not
edited: main's .mule/todo.md compat-note line still calls v1.3.2 the latest stable when v1.3.3
is — left alone rather than hand-edited, because editing the queue file from the Mac while the
mule ticks is the merge-conflict class .mule/MULE.md bans.
