# Candidate issues — 2026-08-24

Distilled from `survey-2026-08-24.md`. Two small candidates; the day's real news is that
main absorbed most of the v3.0 backlog (`ebec5b07..1cee6401`), which **retires** rather than
adds work — see the retirement notes at the bottom. Every candidate was read-confirmed today
against `1cee6401` (GitHub API; this checkout's refs are stale).

---

### keyagreement-acl-residue
Title: Let control/keyAgreement reach the new unknown-control tolerance (or fix the stale deny comment)
Scope: mule-line
Body:
#51's minimum obligation ("tolerate unknown control paths gracefully") landed engine-side in
`06cb17a7`: unhandled control subpaths now get a Warn + Ack (`internal/engine/control.go:46-48`)
instead of an error. But `<realm>/<device>/control/keyAgreement` never reaches that code — the
broker ACL still denies it explicitly, and the deny comment (`internal/broker/aclhook.go:62-68`,
untouched by the wave) justifies the deny with upstream's superseded doc wording ("reserved and
routed correctly, but not yet implemented"). Upstream's current protocol doc
(080-mqtt-v1-protocol.md:172-176, added post-rc.2) instead promises publishes there are
**"accepted and acknowledged"**, and upstream master routes them to its DEK handler. Net: a
spec-following device gets a broker denial from Astrate where current-upstream-doc behaviour is
an ack. The fix is one ACL branch allowing `control/keyAgreement` to fall through to the
tolerant default arm (flip the `"pub control keyAgreement"` test row in `aclhook_test.go`
from deny to accept), plus rewriting the comment to cite the current doc section and #51.
Full DEK/encrypted-endpoint support stays parked per #51; the mapping-field side remains #67.

### stale-auto-queue-lines
Title: Tick the two `[auto]` todo lines already covered by closed issues #47/#48
Scope: mule-line
Body:
`.mule/todo.md` on main still queues `purge-properties-compression-capability [auto]` and
`empty-introspection-verification [auto]`, but both are implemented and their tracking issues
are closed: capabilities handling validates and stores `purge_properties_compression_format`
(zlib|plaintext) in `internal/engine/capabilities.go` (#47), and empty introspection is
tolerated (#48, conformance-tested in `094d5c18`). Each open line risks costing a refill tick
from the daily budget. One commit ticking/annotating both lines with their issue numbers; no
code. While touching the file: `compat-note-v1.3.2 [auto]` calls v1.3.2 "latest stable" — it
is v1.3.3 since 2026-08-07, whose single changelog entry (the resend-failure error-name split)
is exactly what that compat-note run should cover when it executes.

---

## Retirement notes for triage (mapped yesterday, changed today)

- **Dormant-trigger-types decision item** (for-giulio deferred entry + #20 benchmark gate):
  overtaken — `6bd14a77` implements all five change-derived conditions with fixtures and
  integration tests. Retire the decision item; check whether #20 should close or shrink to a
  performance-report-only task.
- **empty-cache/resend-silence → #57 fold question**: still unresolved and still standing
  (re-verified today), now sharpened — upstream ≥v1.3.3 splits load-vs-send failure names,
  Astrate emits neither.
- **capabilities-malformed-disconnect semantics**: with #47/#50 closed, open **#57 is the only
  remaining home** for deciding reject-and-keep vs force-disconnect. Assign it there or lose it.
- **unset_not_allowed mis-map / missing `decryption_error`+`malformed_capabilities_message` /
  BSON-null rejection**: all three re-verified standing today; homes unchanged from
  `issues-2026-08-23.md`'s mapping (#57-confirm ×2, homeless ×1).
- **Group-scoped triggers**: still accepted-but-not-evaluated (`match.go:394,466` today);
  stays parked pending bench group provisioning for trigger matching specifically — AppEngine
  group surfaces landing (#52–#54/#59) does not cover it.
