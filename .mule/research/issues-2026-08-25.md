# Candidate issues — 2026-08-25

Distilled from `survey-2026-08-25.md`. Four small candidates; the day's real news is
closure — #57/#69/#77/#79/#85/#20 all closed and the dormant-trigger decision struck on
main (`0376b839`) — which retires yesterday's decision notes but orphans two standing gaps
and the keyAgreement residue (see retirement notes). Every candidate was grep/read-confirmed
today against raw files at `0376b839`; upstream citations re-fetched at master `372a2cbe`
(byte-identical to yesterday's pin).

---

### keyagreement-acl-residue
Title: Let control/keyAgreement reach the engine's unknown-control tolerance (or cite why deny wins)
Scope: mule-line
Body:
#51 closed as completed on its minimum obligation ("tolerate unknown control paths
gracefully"), and the engine honours it — unhandled control subpaths get Warn + Ack
(`internal/engine/control.go` default arm). But `<realm>/<device>/control/keyAgreement`
never reaches that code: the broker ACL allowlist (`internal/broker/aclhook.go:62`) omits it,
so a spec-following device gets a **broker denial** where upstream's current doc promises
publishes there are "accepted and acknowledged by Astarte, but no response is sent". The deny
comment (`aclhook.go:65-68`, verified verbatim today) quotes only the same section's other
sentence ("reserved and routed correctly, but not yet implemented") and concludes deny
"matches upstream's own status" — upstream's own section says both things, and the ack promise
is the operative wire behaviour. With #51 closed nothing owns this residue. Fix: one ACL
branch letting `control/keyAgreement` fall through to the engine's tolerant default arm (flip
the `"pub control keyAgreement"` test row in `aclhook_test.go` from deny to accept), plus
rewriting the comment to cite both sentences. Full DEK/encrypted-endpoint support stays parked;
the mapping-field side remains #67.

### stale-auto-queue-lines
Title: Tick the two `[auto]` todo lines already covered by closed issues #47/#48
Scope: mule-line
Body:
`.mule/todo.md` on main still queues `purge-properties-compression-capability [auto]` and
`empty-introspection-verification [auto]`, but both are implemented and their tracking issues
are closed: capabilities handling validates and stores `purge_properties_compression_format`
(zlib|plaintext) in `internal/engine/capabilities.go:14-20,77` (#47), and empty introspection
is tolerated in `internal/engine/introspection.go:96-101` (#48, conformance-tested in
`094d5c18`). Each open line risks costing a refill tick from the daily budget. One commit
ticking/annotating both lines with their issue numbers; no code. While touching the file:
`compat-note-v1.3.2 [auto]` calls v1.3.2 "latest stable" — it is v1.3.3 since 2026-08-07
(re-confirmed via tags/releases API today), whose single changelog entry (the resend-failure
error-name split) is exactly what that compat-note run should cover when it executes.

### errorname-table-alignment
Title: Fix unset_not_allowed mapping and add decryption_error/malformed_capabilities_message to the errorname table
Scope: mule-line
Body:
`internal/engine/triggers/errorname.go` still maps `unset_not_allowed` into the
`unexpected_value_type` bucket (`errorname.go:62`, md5-checked unchanged again today) instead
of its own name, and still lacks `decryption_error` and `malformed_capabilities_message`
entirely. These were parked as "#57-confirm" rows, but #57 closed 2026-08-24 without touching
this file, leaving the corrections homeless. The table edit itself is mechanical; the one
judgment call — confirming each correct target string against what upstream actually emits
(v1.3.x name set) — belongs in the task, so verify against upstream before editing rather
than trusting memory. Add test rows mirroring the existing mapping-table tests.

### resend-failure-device-error
Title: Emit device_error on properties-resend failures; add upstream v1.3.3 load-vs-send split
Scope: mule-spec
Body:
`resendServerProperties` (`internal/engine/control.go:144`) logs-and-skips every failure mode
silently — uninstalled interface (`:153`), unmatched path (`:159`), non-rehydratable value
(`:165`), non-encodable value (`:171`) — so a device whose emptyCache resync partially fails
sees nothing; Astrate maps `resend_interface_properties_failed` / `empty_cache_error` but
never emits either. Upstream ≥v1.3.3 additionally splits load-failure from send-failure during
resend (`interface_loading_failed` vs `resend_interface_properties_failed`, d8592091e); Astrate
emits neither. Yesterday's "fold into #57?" question resolved by events: #57 closed without
it. Behavioural addition (new device-visible events), hence mule-spec not mule-line: pick per-
failure-mode names against the measured upstream split, decide whether store errors also surface
or keep propagating to the caller's retry loop, and cover with an integration test.

---

## Retirement/re-home notes for triage

- **Dormant-trigger decision line + #20**: fully resolved on main — `0376b839` strikes the
  for-giulio line citing #20's bench closeout (~0.13 ms keyed read/message, −0.7% throughput
  worst case) and **#20 itself is closed**. Nothing left to triage; drop entirely.
- **Malformed-capabilities disconnect semantics**: behaviour standing (reject-and-keep vs
  upstream force-disconnect), but its home died — #57 closed yesterday without deciding it,
  capabilities.go untouched by the delta. Needs a fresh home: a new for-giulio
  design-decision entry, or attach to future capabilities work. Triage call.
- **empty-cache/resend-silence fold question**: answered by events (#57 closed unfused) —
  elevated above as candidate 4.
- **errorname "#57-confirm" homes**: dead — folded into candidate 3.
- **Group-scoped triggers**: unchanged and deliberately parked (`match.go:394,466` re-read
  today); `0376b839` leaves the for-giulio group line deferred, noting the bench data makes
  it feasible a fortiori. Existing decision entry stands.
- **BSON-null individual value rejection**: pin re-read at `payload_test.go:~499`; still
  homeless, carried another day rather than displacing a candidate.
