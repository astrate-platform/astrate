# Survey — cross-project gap analysis (2026-08-26)

Incremental run against `survey-2026-08-25.md`. Sources and today's pins: Astrate `main`
`0376b839` → **`9c4d411f`** (2026-08-25 17:48 UTC, 2 commits — vdp auto-register + #67
fase 4a), read via read-only GitHub API/raw fetches (origin is SSH-only and this checkout's
keys have no access; no local ref was modified). Upstream `astarte-platform`: `master`
**moved** `372a2cbe` → **`ac04ca95`** (1 commit, dated 2026-07-08 but pushed after
yesterday's pin), tags still top out at `v1.3.3` / `v1.4.0-rc.5`. `mule/queue` **unchanged**
at `28f3adf5`. `atomvm/AtomVM` **unchanged** at `0220c78`.

**Process note:** only the two moving sources were read in depth, per the scoping rule.
Upstream was shallow-cloned into `.mule/research/upstream/astarte` (`--shallow-since`
covering both pins; fetch-by-SHA of the old pin is refused by GitHub) and the delta read
from the clone; AtomVM needed no clone (`ls-remote` identical to pin). Every standing row
below was re-checked on raw files at `9c4d411f` today, not carried blind. `.mule/research/
upstream` cleaned at end of run.

---

## What moved: astrate `0376b839..9c4d411f` — two flow/schema commits

- **`84ae2b63` (#84) — dynamic `virtual_device_pool`**: optional `auto_register` flag;
  first-seen device ids register through the pairing door (`flow.Deps.Register` +
  `ErrVirtualDeviceRegistered`, flowapi pass-through, pairing service built before the flow
  runtime in `cmd/astrate/main.go`). Mirrors upstream's measured semantics — non-registrable
  ids log "cannot register device" and drop with **no negative caching** (every later
  message re-attempts); other register failures are infrastructure errors. Minted secret
  discarded (secrets stay server-side → upstream's DETS-loss brick cannot exist). New
  COMPATIBILITY.md **deviation 16** documents the divergence (engine ingest path instead of
  per-device MQTT sessions, introspection gate skipped, re-registration parity preserved at
  semantic level). Concurrency-guarded first-seen registration tested.
- **`9c4d411f` (#67 fase 4a) — `required`+`encrypted` mapping fields parse and gate**:
  upstream 1.4 experimental attributes adopted per `docs/UPSTREAM-EXPERIMENTAL.md` — parsed
  on datastream mappings, rejected on properties, immutable across minor upgrades
  (`CheckMinorUpgrade` rows added), compiled into `CompiledMapping`. Commit message states
  "Runtime enforcement of required is a later phase" — that phase lives inside **open #67**
  ("decide handling of required and encrypted mapping fields"), so no new home needed; the
  survey records the phase landing only. Delta adds zero TODO/FIXME markers (patch grep).

## What moved: upstream `372a2cbe..ac04ca95` — one commit, encrypted-endpoints telemetry

`ac04ca9` "chore: add metrics for encrypted endpoints feature monitoring" — telemetry only,
no wire-visible change: `realm_dek_status` gauge on the per-realm DEK cache (set/not_set/
failed, incl. TTL-expiry deletions via a ConCache callback), `device_key_agreement_
succeeded/_failed` counters, `encrypted_message_handling_failure` counter tagged by reason.
Material for parity despite being a chore:

1. **The handshake is real now.** The instrumented code paths confirm master implements a
   working device↔Astarte key agreement state machine on `<realm>/<device>/control/
   keyAgreement/4` (exchange succeeded/failed arms in `control_handler.ex`, DEK derivation
   through `astarte_secrets`, encrypted-message handling in `data_handler.ex`). The MQTT-v1
   protocol *doc* still carries, verbatim at master (re-read in the clone today), both old
   sentences — "reserved and routed correctly, but the handshake protocol is not yet
   implemented" (line 174) and "accepted and acknowledged by Astarte, but no response is
   sent until the feature is fully implemented" (line 176). The doc lags its own code.
   This makes Astrate's `aclhook.go:65-68` deny comment stale against **both** halves: it
   cites only the "not yet implemented" sentence, and its conclusion that deny "matches
   upstream's own status" no longer matches upstream's actual behaviour either → candidate 1
   strengthened.
2. **Disconnect-on-decrypt-failure confirmed in code**: both encrypted-message failure kinds
   (`key_agreement_error`, `decryption_error`) call `Core.Error.handle_error(...,
   ask_clean_session: true)` — i.e. force-disconnect — with an inline upstream TODO about
   softening it. Relevant context for the malformed-capabilities disconnect decision and for
   candidate 3's verification step (upstream distinguishes these names internally; whether
   they ever appear as device-visible error_names must be checked before editing the table).

## Standing-gap census re-verified today against `9c4d411f`

Each row re-checked via raw reads, not carried:

- **errorname set gaps — STANDING, homeless**: `internal/engine/triggers/errorname.go`
  unchanged: `unset_not_allowed` still maps into the `unexpected_value_type` bucket
  (`errorname.go:62`); `decryption_error` / `malformed_capabilities_message` still absent
  (grep). No open issue covers them (#57 closed without touching this file) → candidate 3.
- **Properties-resend failure silence — STANDING, homeless**: `resendServerProperties`
  (`control.go:144`) still logs-and-skips all four failure modes (`control.go:155`, `161`,
  `167`, `173` — line numbers re-measured today; file untouched by the delta, yesterday's
  `:153/159/165/171` were off by two) emitting no `device_error`; the mapped-but-never-
  emitted `resend_interface_properties_failed` / `empty_cache_error` stand, as does
  upstream ≥v1.3.3's load-vs-send split → candidate 4.
- **Malformed-capabilities disconnect semantics — STANDING, home lost**: unchanged; still
  no open issue covers it; yesterday's proposed for-giulio design-decision entry has not
  been written to main's `.mule/for-giulio.md` (re-read today) → re-home note stands for
  triage.
- **keyAgreement wire divergence — residue homeless and now stronger**: engine-side
  tolerance stands (`control.go` default arm Warn+Ack); broker ACL still denies first
  (`aclhook.go:65-68`); test row `aclhook_test.go:53` still flips-deny. Upstream movement
  above sharpens the fix's justification → candidate 1.
- **BSON-null individual value rejection — STANDING**: pins re-read at
  `payload_test.go:363` (`{"v":null}` → `ReasonTypeMismatch`) and `:499` ("null v
  rejected", raw BSON TypeNull). Still homeless; carried, not elevated (cap discipline).
- **Group-scoped triggers — parked, untouched**: `noteUnsupported` re-read at
  `match.go:393-394,465-466`; delta did not touch match.go. Existing for-giulio deferral
  (tied to #17) unchanged.
- **TODO census — one sanctioned marker**, the lttb toolkit extension point
  (`store.go:139-145`), re-read today; delta introduced zero markers.

---

## Source 2 — mule queue

`mule/queue` unchanged at `28f3adf5`. `.mule/log.md` task table in this checkout still
header-only, zero rows. `.mule/todo.md` re-read at `9c4d411f`: `purge-properties-compression-
capability [auto]` and `empty-introspection-verification [auto]` still open although #47/#48
are closed and the work is greppable (re-confirmed today: `capabilities.go:14-20` zlib|
plaintext constants, `introspection.go:96-101` empty-payload-is-valid), `compat-note-v1.3.2
[auto]` still calls v1.3.2 latest stable though v1.3.3 published 2026-08-07 (tags API:
still top stable tag) → candidate 2.

## Source 3 — upstream `astarte-platform`

One-commit delta (see above). Tags unchanged (`v1.3.3` newest stable, `v1.4.0-rc.5`
newest rc). Clone read for the Key Agreement doc section and the DUP diff; nothing else in
the delta touches docs.

## Source 4 — parity gap table delta

Rows closed: virtual_device_pool dynamic semantics (+deviation 16); mapping-field parsing
for the 1.4 experimental pair. Rows narrowed: none further. Rows sharpened: keyAgreement
ACL residue (upstream handshake now demonstrably live behind an outdated doc). Rows opened:
none wire-visible yet — the encrypted-endpoints surface (DEK cache semantics, exchange
protocol on `keyAgreement/4`, decrypt-failure disconnect policy) is real on master but ships
only in 1.4.0, still rc-only. Watch note for triage: when v1.4.0 lands, Astrate's parsed-
but-inert `Encrypted` flag and absent DEK machinery become a full parity work area; #67
already owns the attribute-handling slice.

## Source 5 — upstream `atomvm/AtomVM`

Unchanged (`0220c78`; ls-remote identical to yesterday's pin). Zero possible delta; clone
skipped. §3.5 JSON-payload-profile rationale stands; no work item.
