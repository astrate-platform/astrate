# Candidate issues — 2026-08-23

Distilled from `survey-2026-08-23.md`. **Zero new candidates**: upstream `astarte-platform`
and AtomVM did not move, and astrate `main`'s delta (`4c881d0f`..`ebec5b07`, flow
lifecycle + milestone bookkeeping) touches no wire-visible behaviour.

What did change is structural: commit `081abe4` filed the v3.0 upstream-parity backlog on
the main repo (**#47–#89**, decided 2026-08-22). Yesterday's carried candidates therefore
get **mapped, not restated** — triage should retire them from this daily pipeline once it
confirms each home below, so they stop being re-carried day after day. Every carried
`mule-line`/`mule-spec` item was nonetheless re-grep-confirmed today against `ebec5b07`
as still unimplemented (survey Source 1), so none of these retirements mean the gap closed.

---

## Candidate → backlog mapping (retire after confirming)

- **encrypted-mapping-attribute** → **#67** ("decide handling of required and encrypted
  mapping fields (upstream 1.4)"). Title matches exactly; yesterday's candidate adds the
  implementation detail (strict parser rejects at `pkg/interfaceschema/parse.go:121`,
  fix = optional `Encrypted *bool` + documented unsupported-encryption note) if #67's body
  lacks it.
- **key-agreement-protocol**, **encrypted-endpoints** → **#51** ("keyAgreement control
  topic + encrypted mappings (upstream 1.4 experimental) — investigate/park"). Backlog
  parks it; yesterday's candidates treated key agreement as prerequisite chain for
  endpoints — consistent with "investigate/park". The 08-22 correction A (comment citation
  stale at `internal/broker/aclhook.go:63-68`) belongs in #51's body.
- **capabilities-malformed-disconnect** → closest homes are **#50** (discard malformed
  introspection/purge/capabilities instead of crashing) and **#47** (capabilities
  acceptance); the `malformed_capabilities_message` name belongs to **#57**'s taxonomy
  audit. Note the semantics differ: upstream force-disconnects + emits the error name,
  while Astrate rejects-and-keeps (`handleCapabilities`, `capabilities.go:29`;
  `DisconnectDevice` unused from that path, `broker.go:260`). Triage should decide which
  issue owns disconnect semantics so it isn't lost between #47/#50/#57.
- **upstream-error-names-new** → within **#57** ("audit server-write error taxonomy").
  `decryption_error` / `malformed_capabilities_message` absence re-confirmed today
  (`errorname.go` grep count 0).
- **unset-not-allowed-mapping** → likely #57 but **not visible in its title** — confirm
  before closing here. Gap re-verified open: `errorname.go:62` mis-map still pinned by
  `errorname_test.go:30`.
- **empty-cache-error-triggers** → **not visibly covered by any #47–#89 title** — flag
  for triage: fold into #57 or keep as its own item. Gap re-verified open
  (`control.go:58-76`; mapped-but-never-emitted names at `errorname.go:20-21`).
- **bson-null-value-acceptance** → **not visibly covered by any title** — same treatment.
  Evidence stable: rejection pinned at `pkg/payload/payload_test.go:499`.

**Deferred stays deferred:** group-scoped-triggers remains parked pending bench group
provisioning (#53/#54 cover AppEngine group surfaces, not device/data trigger matching;
`match.go:318,354` untouched).

---

## Still standing, human-action (unchanged)

- **#10 premise re-check** — its "external-bus intake TODO at store.go:135" premise does
  not hold on main (phantom TODO; survey-2026-08-22 correction B). Still needs a human
  decision before acting on it either way.
- **#17 / #18 / #19** — unverified live-upstream recordings (`COMPATIBILITY.md` deviation 1
  and line 122 items). Unchanged; still the "architect not unsupervised agent" trap.
