# Survey — cross-project gap analysis (2026-08-24)

Incremental run against `survey-2026-08-23.md`. Sources and today's pins: Astrate `main`
`ebec5b07` → **`1cee6401`** (2026-08-23 21:07, 32 commits — the v3.0 parity-backlog wave),
read via read-only GitHub API because this checkout's remote-tracking refs are stale
(its freshest ref predates the wave; nothing local was modified). Upstream `astarte-platform`:
`master` **unchanged** at `372a2cbe`, tag scan shows nothing newer than `v1.3.3` /
`v1.4.0-rc.5`. `mule/queue` **unchanged** at `28f3adf5`. `atomvm/AtomVM` **unchanged** at
`0220c78` (same tip since 2026-07-13). Both upstream clones were made and deleted per the
cleanup rule; the upstream delta below was re-derived from them before confirming there was
no delta to scope to.

**Process note:** this run first misidentified its baseline as `survey-2026-07-27.md` — the
2026-07-28..08-23 output files were not present in the worktree at start and materialized
mid-run. The first draft of today's files treated v1.3.3, the rc-series, Encrypted Endpoints,
and the resend-silence/keyAgreement rows as new findings; every one of those was already
known (standing rows in the 08-21/08-22/08-23 surveys, filed across #46–#90). The files were
rewritten against the correct baseline; what follows is only the true delta.

---

## What moved: `ebec5b07..1cee6401` — the v3.0 backlog lands

Yesterday's survey reported main at `ebec5b07` with the parity backlog *filed* (#47–#89).
Today most of that backlog is *implemented* on main. Grouped by surface:

- **Trigger engine**: change-derived data conditions now evaluated — `value_change`,
  `value_change_applied`, `path_created`, `path_removed`, `value_stored` (`6bd14a77`; testdata
  fixtures + integration tests). This **closes the longest-standing deliberate gap** (the
  "accepted-but-never-firing" five) and overtakes issue #20's benchmark gate.
  Wildcard semantics + introspection rules (`ed0342d6`, #70); HTTP action validation limits
  (`edf4e1b0`, #63); policy handler-disjointness/prefetch bounds/AMQP reject (`029e0b6e`,
  #64/#65).
- **MQTT v1 / engine**: unknown control subpaths tolerated silently — logged Warn + Ack in
  `control.go:46-48` (`06cb17a7`, executing #51's minimum obligation at the engine level);
  empty-introspection tolerance + binaryblob subtype-0 resend conformance (`094d5c18`,
  #48/#49); device-owned ingest path `PublishDeviceValue` (`5e1aa1df`, #84a).
- **AppEngine**: data-query parameter set + formats/disjoint_tables (`ba339fc0`, #55/#56);
  devices-by-alias mirror + group-scoped access (`18315257`, #52/#53); show-group +
  group validation + UUID-v1 pagination (`3ba518f2`, #54/#59); PATCH content-type gate +
  error taxonomy (`7bf08a36`, #58).
- **Realm Management/Housekeeping/Pairing**: validation matrix gaps + detailed listing
  (`c073835c`, #61/#66); measured install/update/delete error taxonomy (`0b5a35c5`, #62);
  retention ceiling install-reject + sweep + migration 000011 (`834d5367`, `56e9b4b6`, #72);
  PATCH realm + default-retention injection (`43f0409e`, #74/#73); realm-deletion gating +
  pairing health (`5111e3c5`, #75/#71); GET retention config (`5d74ad89`, #60).
- **Conformance**: group WATCH shapes, server-owned interface, size limits **measured against
  live upstream** (`7f3a6008`) — this is the recording behind COMPATIBILITY.md's updated
  deviation-1 text (#17/#18/#19 family executed).
- **Flow**: pure-transform block set (`4fa0e3d8`, #80), json_path_map (`7f91d261`, #81),
  http_source/sink (`4d456bd2`, #82), mqtt_source/sink (`abd1556d`, #83), virtual_device_pool
  over ingest (`ad55cab9` + prep, #84), composite blocks with cycle guard + user_blocks
  migration 000012 (`78e293e7`, `8c8804db`, #85), config schemas served from discovery
  (`671982e3`, #89), a_f JWT claim (`55bb7171`, #88).
- **Docs/decisions**: `docs/UPSTREAM-EXPERIMENTAL.md` register + APICompatVersion bump rule
  (`dbd02671`, #67/#68/#90); FDO milestone-4.0 decision recorded (`1cee6401`, #78).

## Standing-gap census re-verified today against `1cee6401`

Each row below was individually re-checked on current main (API reads), not carried:

- **Dormant trigger types — CLOSED** by `6bd14a77` (see above). Still accepted-but-not-
  evaluated: `device_empty_cache_received`, `interface_minor_updated`, and group-scoped
  triggers (`match.go:10`, `noteUnsupported` at `match.go:394,466` — re-read today). The
  deferred for-giulio decision item should be retired by triage.
- **errorname set gaps — STANDING**: `internal/engine/triggers/errorname.go` is byte-identical
  to yesterday (md5-checked today): `unset_not_allowed` still mis-maps to
  `unexpected_value_type`; `decryption_error` / `malformed_capabilities_message` still absent.
- **Properties-resend failure silence — STANDING**: `resendServerProperties`
  (`control.go:144`) still logs-and-skips unresolvable-interface rows (`control.go:153`),
  emitting no `device_error`; `resend_interface_properties_failed` /
  `empty_cache_error` remain mapped-but-never-emitted. Upstream ≥v1.3.3 additionally splits
  load-failure from send-failure during resend (`interface_loading_failed` vs
  `resend_interface_properties_failed`, d8592091e) — Astrate emits neither, so the split has
  no Astrate-side counterpart yet. Home: fold into open #57 or give it its own row (triage
  call flagged yesterday, still unresolved).
- **Malformed-capabilities disconnect semantics — STANDING**: `capabilities.go` untouched by
  the wave; Astrate still rejects-and-keeps where upstream force-disconnects. With #47/#50
  now closed, open **#57 is the only remaining home** for this decision — sharper than
  yesterday's three-way split.
- **BSON-null individual value rejection — STANDING**: `pkg/payload` untouched; rejection
  pinned at `payload_test.go:499`. Still homeless.
- **keyAgreement wire divergence — NARROWED but real** (see issues file): engine-level
  tolerance landed, but the broker ACL still denies the topic before the engine sees it —
  `aclhook.go` was not touched by the wave; deny comment at `aclhook.go:62-68` re-read today,
  still citing the superseded doc wording.
- **TODO census — one sanctioned marker**, and it is the **lttb toolkit** extension point at
  `store.go:~137` ("when timescaledb_toolkit is present…"), re-read today. Yesterday's
  human-action note about #10's phantom external-bus TODO premise stands: #10 is decided and
  no TODO references it.

---

## Source 2 — mule queue

`mule/queue` unchanged at `28f3adf5` (ls-remote == yesterday's pin). `.mule/log.md` task table
in this checkout still has zero rows. Nothing new to filter candidates against beyond the
GitHub tracker state used throughout.

## Source 3 — upstream `astarte-platform`

No movement: `master` HEAD `372a2cbe` (== 08-23 pin), tags top out at `v1.3.3` /
`v1.4.0-rc.5`. The clone's CHANGELOG confirms rc.3/rc.4/rc.5 contents already analysed on
08-21/08-22; `CHANGELOG.md` Unreleased section still has no entry for the Encrypted Endpoints
series (#1999 et al.). Nothing new to report; standing citations hold.

## Source 4 — parity gap table delta

Rows closed by the wave: dormant trigger types; a_ch/group-WATCH measurement debt;
wildcard-semantics audit; HTTP-action limits; policy bounds; AMQP-action reject; RM error
taxonomy; detailed listing; AppEngine query/format/group surfaces; empty introspection;
binaryblob subtype-0. Rows opened: none new from upstream (it did not move). Rows narrowed:
keyAgreement (engine side done, ACL residue left — candidate 1). Rows unchanged-standing:
the five bullets above.

## Source 5 — upstream `atomvm/AtomVM`

Unchanged (`0220c78`, tip dated 2026-07-13 — older than the previous survey, so nothing could
have moved). Zero Astarte references re-confirmed in the fresh shallow clone before cleanup.
The §3.5 JSON-payload-profile rationale stands; no work item.
