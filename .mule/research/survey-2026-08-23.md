# Survey — cross-project gap analysis (2026-08-23)

Sources: Astrate codebase/docs (`main` moved `4c881d0f` → `ebec5b07`, read from a fresh
shallow https clone — this checkout's working tree remains stale and was **not** used),
mule queue history (`origin/mule/queue`, unchanged at `28f3adf5`), upstream
`astarte-platform` (`master` unchanged at `372a2cbe`; **no new tags** — latest still
`v1.3.3` and `v1.4.0-rc.5`), upstream `atomvm/AtomVM` (unchanged, `0220c78`). Every claim
cites a specific path, commit, or doc section.

Incremental note: last survey was `survey-2026-08-22.md`. Since then:

- **Astrate's `main` moved** — 4 commits, all Flow-lifecycle implementation (#44/#45/#46
  phases) plus milestone redefinition. **No changes to `docs/`, `internal/broker/`,
  `internal/engine/`, or `pkg/` anywhere in the range** (`git diff --name-only
  4c881d0f..ebec5b07` checked directly), so every standing parity citation holds with
  line refs unchanged. Details in Source 1; standing-gap re-verification below.
- **Upstream `astarte-platform` did not move** — `ls-remote` HEAD == `372a2cbe`, and a
  full `refs/tags/v1.*` scan shows nothing newer than `v1.4.0-rc.5`.
- **`origin/mule/queue` did not move** — still `28f3adf5`.
- **AtomVM did not move** — still `0220c78`.

## The change that matters more than the code delta

`081abe4` redefines milestone v3.0 as *"upstream Astarte feature parity (1.2.2 →
1.3/1.4)"*, tracked as GitHub backlog **#47–#89** (`upstream-parity` label) plus parked
**#90** (APICompatVersion decision), scope decided by Giulio on 2026-08-22 from a
one-off investigation whose source document
(`.mule/research/upstream-parity-2026-08-22.md`) is local-only — it is **not** on this
branch (checked; ask Giulio if triage needs it).

Effect on this survey's pipeline: yesterday's carried candidates now have filed homes on
the main repo, so restating them would be duplicate noise. Today's `issues-2026-08-23.md`
therefore proposes **zero new candidates** and instead maps yesterday's list onto
#47–#89 for retirement. New-candidate volume drops because the work got filed, not
because gaps closed — every standing row below was re-verified open today.

---

## Source 1 — Astrate codebase and docs (`origin/main` ebec5b07)

### What the four commits did (none change Astarte-wire parity)

- `4e72c04 feat(flowapi): flow reload + config update without delete+recreate (#44, #46)` —
  `POST /flow/v1/{realm}/flows/{name}/reload` (re-resolve pipeline + stored config into a
  fresh live graph; doubles as manual restart) and `PUT .../config` (dry-run substitution
  against the current pipeline, persist snapshot, rebuild live flows);
  `RestartFlowInstance` = teardown + rebuild from the durable row, never deleting it;
  `UpdatePipeline` response now lists `referencing_flows`; store gains
  `UpdateFlowConfig`; design note added at
  `docs/handoff/flow-lifecycle-followups-design-2026-08-22.md`. All Astrate-specific
  Flow-surface behaviour — no MQTT/AppEngine wire interaction.
- `fb3f335 feat(flow): block-death detection + auto-restart with backoff (#45 phase 1)` —
  container block gains an exit watcher (docker wait): unexpected exit while running
  raises `Deps.NotifyFatal`; whole flow marked failed with `failed_block` persisted (new
  `flows.failed_block` column, migration `000010`); `auto_restart` flows rebuild via
  `RestartFlowInstance` with exponential backoff (1s..30s cap). Also Astrate-specific.
- `081abe4 milestones:` — the v3.0 redefinition described above; also records that the
  original v3.0 candidate (remaining SECO suite pieces) was dropped for legal reasons and
  Edgehog compatibility is verified (#28).
- `ebec5b0 docs:` — aligns `.mule/recipes/milestones.md` and the dashboard skill with the
  new v3.0 scope. Bookkeeping.

### Standing gaps re-grep-verified today against `ebec5b07`

The range touches none of these files; each was nevertheless individually re-confirmed
open (grep/sed against the fresh clone):

- `unset_not_allowed` mis-map — `internal/engine/triggers/errorname.go:62` →
  `"unexpected_value_type"`; wrong value still pinned by `errorname_test.go:30`.
- `decryption_error` / `malformed_capabilities_message` — absent from `errorname.go`
  (grep count 0 across the file).
- Malformed capabilities reject-not-disconnect — `handleCapabilities` at
  `internal/engine/capabilities.go:29` rejects via `e.reject` and keeps the connection;
  `Broker.DisconnectDevice` (`broker.go:260`) is still called only from the dashboard
  path (`internal/realm/service.go:99`), never from the engine.
- Key agreement ACL-denied — comment block at `internal/broker/aclhook.go:63-68`
  unchanged; the 08-22 correction A note stands (the comment cites the stale protocol
  doc, while upstream's DUP implements the handshake).

- Encrypted-endpoint machinery absent — `grep -ri 'dek|kek|cose' internal/` matches only
  `fakeKeySource`/`fakeKeys` test doubles (case-insensitive `kek` inside "fakeKey"); no
  real DEK/KEK/COSE code.
- emptyCache resend failure silent — `handleEmptyCache`
  `internal/engine/control.go:58-76`: resend wrapped in `retryStore`, permanent failure
  returns without ack; no device_error trigger, no session end;
  `resend_interface_properties_failed`/`empty_cache_error` remain mapped-but-never-emitted
  (`errorname.go:20-21`).
- BSON-null individual value rejected — pinned by
  `pkg/payload/payload_test.go:499` ("null v rejected", `ReasonTypeMismatch`). Path
  note: the test lives under `pkg/payload/`, not under `internal/` as earlier surveys'
  shorthand implied; same file, same line.
- Group-scoped triggers compile-but-never-match — `noteUnsupported(c.GroupName != "")`
  at `internal/engine/triggers/match.go:318,354`.
- TODO census unchanged: exactly one sanctioned `// TODO` on main
  (`internal/store/store.go:135`, lttb extension point). The only other grep hit,
  `context.TODO()` at `internal/flow/blocks/astartesource/source.go:56`, is not a
  sanctioned marker.

---

## Source 2 — mule queue

`origin/mule/queue` unchanged at `28f3adf5` (ls-remote == last surveyed pin). No new
implementation commits to assess. `.mule/log.md` task table in this checkout still has
zero rows.

---

## Source 3 — upstream `astarte-platform`

`master` unchanged at `372a2cbe` (ls-remote HEAD == pin). Tag scan over `refs/tags/v1.*`
returns nothing newer than `v1.4.0-rc.5`; latest stable still `v1.3.3`. Nothing to diff;
the 08-21 release-branch analysis stands, including the note that `CHANGELOG.md`'s
Unreleased section had not yet generated an entry for #1999.

---

## Source 4 — parity gap analysis

No upstream movement + no broker/engine/payload movement ⇒ **no row added, none closed**.
The standing table is byte-for-byte yesterday's (`survey-2026-08-22.md` Source 4), with
all Astrate-side rows re-verified above against `ebec5b07`. The only change is
administrative but structural: the rows now have filed homes in the v3.0 backlog — see
`issues-2026-08-23.md` for the candidate → issue mapping (#51 keyAgreement + encrypted
mappings, #67 encrypted/required mapping fields, #50 malformed-message hardening, #57
error taxonomy audit).

---

## Source 5 — upstream `atomvm/AtomVM`

Unchanged (`0220c78`, same since 2026-07-13). Per recipe, the connection was established
once and nothing today changes its conclusion: AtomVM matters to Astrate only as a target
platform for the JSON payload profile (`docs/DESIGN.md` §3.5); no AtomVM-specific work
item. Not re-derived.
