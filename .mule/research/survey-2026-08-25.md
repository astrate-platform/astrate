# Survey — cross-project gap analysis (2026-08-25)

Incremental run against `survey-2026-08-24.md`. Sources and today's pins: Astrate `main`
`1cee6401` → **`0376b839`** (2026-08-24 14:28 UTC, 10 commits — the verify-batch closeout),
read via read-only GitHub API/raw fetches (origin is SSH-only and this checkout's keys have
no access; no local ref was modified). Upstream `astarte-platform`: `master` **unchanged**
at `372a2cbe`, tags top out at `v1.3.3` / `v1.4.0-rc.5` (releases feed confirms rc.5,
2026-08-20, is newest). `mule/queue` **unchanged** at `28f3adf5`. `atomvm/AtomVM`
**unchanged** at `0220c78` (tip dated 2026-07-13).

**Process note:** both upstream clones were skipped this run — `ls-remote` HEADs are
byte-identical to yesterday's pins, so a clone is delta-free by definition; per the scoping
rule only the astrate main delta was read. `.mule/research/upstream` was still cleaned per
the rule (absent at end of run).

---

## What moved: `1cee6401..0376b839` — verify batch lands, five issues close

Yesterday's survey reported the v3.0 wave absorbed and the conformance recording in flight.
Today the 2026-08-24 verify batch (#69/#77/#79/#57) landed with its fixtures and docs, Flow
#85 finished its last three phases, and the dormant-trigger decision was struck on main:

- **Flow #85 finished**: per-realm `user_blocks` CRUD in the store (`90254047`; store_test
  migration assertion bumped 11→12), jsonschema/v6 dependency (`4dec3280`), flowapi
  user-block CRUD endpoints with schema validation (`4b6dfc70`, new `userblocks.go`),
  composites accepted and expanded through the pipeline lifecycle (`14708db5`). **Issue #85
  closed.**
- **Verify batch recorded + executed**: `verify-server-writes.json` /
  `verify-versions.json` (+transcripts) fixtures from scratch probes, envelope "Bad request"
  casing corrected to the measured form (`907b6e74`, #69/#57/#77/#79 — **all four closed**).
- **AppEngine server-write taxonomy matched upstream** (`ad4eb45b`, #57 → **closed**):
  64 KiB value cap answers measured `422 Value size exceeds size limits`; engine sentinels
  (`ErrNotServerOwned`/`ErrNotAProperty`/`ErrInterfaceNotFound`/`ErrPathNotFound`) mapped
  ahead of the store `ErrNotFound` catch-all with an explicit first-match ordering guard;
  unknown causes stay 500.
- **COMPATIBILITY.md**: deviation 14 (measured write taxonomy) and 15 (canonical detail
  casing follows measurement) added (`ab31319b`), including two deliberate divergences —
  wrong-scalar-type/malformed-aggregate kept bug-for-bug 500, DELETE on a server-owned
  *individual* datastream answered with the clean 405 form — plus the measured fact that the
  REST write path has no offline-push failure, and the unknown-realm `403`-vs-Astrate-`401`
  deviation under the auth bullet (#69). "Internal Server Error" → "Internal server error"
  everywhere.
- **Docs/process**: AGENTS.md gains the hard Mac/Docker rule (container work is Legion-only;
  container-free `go test` fine — quote precisely in every handoff, `1e4d4d5c`);
  conformance README gains the probe-craft trap list (unpadded base64url device IDs, single
  envelope wrap, empty-introspection-first trap, per-service claim keys
  `a_rma`/`a_aea`/`a_pa`/`a_ha`/`a_ch`, realm pubkey drift after re-provision, SNI hostname
  for MQTT tunnels, upstream's own unmapped 500 rows are findings to record then decide).
- **Bench closeout**: the dormant-trigger decision line struck through in
  `.mule/for-giulio.md` citing #20's Legion Go bench (~0.13 ms keyed read/message, −0.7%
  throughput worst case); **#20 itself closed** (`0376b839`).

## Standing-gap census re-verified today against `0376b839`

Each row re-checked on current main via raw reads, not carried:

- **errorname set gaps — STANDING, now homeless**: `internal/engine/triggers/errorname.go`
  unchanged (md5 `d417b8e17e052ac9cf9b27020ab934e1`): `unset_not_allowed` still maps into
  the `unexpected_value_type` bucket (`errorname.go:62`); `decryption_error` /
  `malformed_capabilities_message` still absent. The planned home (#57-confirm) died
  yesterday when #57 closed without touching this file → candidate 3.
- **Properties-resend failure silence — STANDING, now homeless**: `resendServerProperties`
  (`control.go:144`) still logs-and-skips unresolvable rows (`control.go:153`, `159`, `165`,
  `171`) emitting no `device_error`; `resend_interface_properties_failed` /
  `empty_cache_error` remain mapped-but-never-emitted; upstream ≥v1.3.3 splits load-vs-send
  failure during resend (`interface_loading_failed` vs `resend_interface_properties_failed`)
  and Astrate emits neither. Yesterday's fold-into-#57 question is answered by events: not
  folded, #57 closed → candidate 4.
- **Malformed-capabilities disconnect semantics — STANDING, home lost**: behaviour
  unchanged (Astrate rejects-and-keeps where upstream force-disconnects); with #47/#50/#57
  all closed there is **no open issue covering it** → re-home note for triage below.
- **keyAgreement wire divergence — NARROWED further, residue homeless**: engine-side
  tolerance stands (`control.go` default arm Warn+Ack), but the broker ACL still denies the
  topic first (`aclhook.go:65-68`), and the deny comment quotes only the "not yet
  implemented" sentence of upstream's Key Agreement section while the same section promises
  publishes are **"accepted and acknowledged"** (re-fetched from master today — both
  sentences verified verbatim). With #51 closed-completed (2026-08-22) nothing open covers
  the residue → candidate 1.
- **BSON-null individual value rejection — STANDING**: pin re-read at
  `payload_test.go:~499` ("null v rejected" → `ReasonTypeMismatch`). Still homeless; carried,
  not elevated (cap discipline).
- **Group-scoped triggers — parked, untouched**: `noteUnsupported` at `match.go:394,466`
  re-read today; delta did not touch match.go. `0376b839` explicitly leaves the group line
  deferred, noting the bench data makes it a fortiori feasible — decision still wanted, but
  that is the existing for-giulio entry, unchanged.
- **TODO census — one sanctioned marker**, the lttb toolkit extension point
  (`store.go:139-145`), re-read today. The delta introduced zero TODO/FIXME markers (patch
  grep).

---

## Source 2 — mule queue

`mule/queue` unchanged at `28f3adf5` (API branch tip == yesterday's pin). `.mule/log.md`
task table in this checkout still header-only, zero rows. `.mule/todo.md` on main
re-read today: both stale `[auto]` lines are still open (`purge-properties-compression-capability`,
`empty-introspection-verification`) although their issues (#47/#48) are closed and the work
is greppable (`capabilities.go:14-20,77` validates zlib|plaintext; `introspection.go:96-101`
treats the empty payload as a valid empty introspection), and `compat-note-v1.3.2 [auto]`
still calls v1.3.2 latest stable though v1.3.3 published 2026-08-07 → candidate 2.

## Source 3 — upstream `astarte-platform`

No movement: `master` HEAD `372a2cbe` (== 08-23/08-24 pins), tags top out at `v1.3.3` /
`v1.4.0-rc.5`. Clone skipped (see process note); standing citations hold. One targeted doc
read was done anyway for candidate 1: `doc/pages/architecture/080-mqtt-v1-protocol.md` Key
Agreement section at master contains both the "reserved and routed correctly, but the
handshake protocol is not yet implemented" note **and** the "accepted and acknowledged by
Astarte, but no response is sent" promise — Astrate's deny comment cites only the former.

## Source 4 — parity gap table delta

Rows closed by this batch: server-write error taxonomy; per-service version endpoints;
unknown-realm status measurement; registration-limit measurement; fase-1 verify fixtures;
Flow user blocks + composites lifecycle. Rows narrowed: keyAgreement (now an ACL-only
residue with no owning issue — candidate 1). Rows opened: none (upstream static). Homes
reshuffled: two standing gaps lost their #57 home (candidates 3–4); disconnect-semantics
decision needs a fresh for-giulio entry.

## Source 5 — upstream `atomvm/AtomVM`

Unchanged (`0220c78`, tip dated 2026-07-13; ls-remote identical to pin). Zero possible
delta; clone skipped. §3.5 JSON-payload-profile rationale stands; no work item.
