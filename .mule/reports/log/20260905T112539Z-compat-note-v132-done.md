slug: compat-note-v132
verdict: done
at:  4bc3e1a
ran: 2026-09-05T11:25:39Z on DietPi in 73s


> build · big-pickle

→ Read .mule/MULE.md
→ Read .mule/task.md
→ Read docs/COMPATIBILITY.md
The task is: propose wording for `docs/COMPATIBILITY.md` for upstream Astarte v1.3.2, in `.mule/for-giulio.md`. I must NOT edit `docs/COMPATIBILITY.md` directly.

Let me read the current compatibility doc and the existing for-giulio file.
→ Read .mule/for-giulio.md
Now I need to understand what changed in upstream Astarte v1.3.x. Let me look at the upstream releases and Astrate's current version mapping.
% WebFetch https://github.com/astarte-platform/astarte/releases/tag/v1.3.2
% WebFetch https://github.com/astarte-platform/astarte/releases/tag/v1.3.0
✱ Grep "APICompatVersion|1\.2\.2|upstream.*version" 18 matches
% WebFetch https://github.com/astarte-platform/astarte/releases/tag/v1.3.1
→ Read internal/realm/service.go [offset=575, limit=20]
Now I understand the context. Astrate targets upstream v1.2.2 (APICompatVersion = "1.2.2"). The task asks me to propose COMPATIBILITY.md wording for upstream v1.3.2 (latest stable; v1.4.0 is rc-only).

The existing COMPATIBILITY.md is written from the perspective that Astrate targets upstream **1.2.2**, with many measurements against 1.2.0. Upstream v1.3 introduced several **BREAKING** changes (per the v1.3.0 release notes), notably:
- Merged API services into main services
- AMQP vhost per realm, mandatory AMQP producer config
- MQTT capabilities / `purge_properties_compression_format` capability
- Allow devices with empty introspection
- Device registration triggers, device deletion started/completed triggers
- FDO auth (experimental)
- Realm-scoped health checks (FDO path)

But Astrate is a wire-compatible re-implementation targeting the **client-visible wire surface**. Let me consider what the wording update should actually propose. The task says "propose the docs/COMPATIBILITY.md wording update for upstream v1.3.2 (latest stable; v1.4.0 is still rc-only)".

The intent is: since v1.3.2 is now the latest stable upstream (and Astrate's doc references v1.2.2), how should COMPATIBILITY.md be updated to reflect the v1.3.2 reality. But I must not edit COMPATIBILITY.md; I write a proposed wording into `.mule/for-giulio.md` for Giulio's decision.

Given COMPATIBILITY.md's structure, the key point is Astrate's targeted upstream version is 1.2.2 (APICompatVersion). The doc doesn't claim full v1.3 support. So the proposal should note that v1.3.x introduces client-visible changes that Astrate does not yet emulate, and this is a decision point for whether v1.3.2 becomes the new compatibility target.

Let me check what client-visible changes are actually relevant to wire compatibility. From the release notes, the relevant wire-surface items for v1.3.0:
- MQTT capabilities (devices declare optional features) + `purge_properties_compression_format` capability (plaintext compression)
- Empty introspection allowed
- Device registration triggers (pairing)
- Device deletion started/completed triggers (RM)
- FDO auth (experimental, pairing)
- Realm-scoped health check `GET /pairing/v1/{realm}/health` — wait, Astrate already implements this (deviation 18 references #71 and that upstream 404s it). Actually the release note says v1.3.0 ADDED "Realm-scoped health checks, which can be used to test astarte health or connectivity by devices who are only aware of realm-scoped paths, such as during FDO". So upstream 1.3 ADDED `GET /pairing/v1/{realm}/health` — which Astrate already serves (deviation 18 says "Astrate additionally keeps the realm-scoped GET /pairing/v1/{realm}/health (#71), which upstream 404s"). So on 1.3 upstream now has this route too! That's a relevant change — Astrate's deviation 18 note "which upstream 404s" becomes inaccurate against 1.3.

Let me verify what path upstream 1.3 uses. The release note lists "Realm-scoped health checks". Let me check the upstream pairing health path. Let me search.

Let me look at how Astrate implements the realm health route and check upstream 1.3.0's path.
✱ Grep "health" in internal/pairing · 12 matches
→ Read .mule/recipes/milestones.md
The Astrate contract realises that upstream 1.3+ added the realm-scoped health. Now the compatibility doc references upstream 1.2.2 as its target throughout. My task is narrow: propose COMPATIBILITY.md wording for v1.3.2 in for-giulio.md, not edit the doc.

Let me understand the full picture. The doc is about wire-compatibility deviations and pinned clients. Upstream v1.3.x changes that touch the wire surface Astrate documents:

1. **MQTT capabilities** — devices can declare support for optional features; `purge_properties_compression_format` capability (plaintext vs zlib). This is an additive MQTT-level feature. Astrate's COMPATIBILITY.md deviation 1 discusses `emptyCache`/`producer/properties`/`consumer/properties` with zlib + size-prefixed payloads. The `purge_properties_compression_format` capability would be a new wire element.
2. **Empty introspection allowed** — previously a device with empty introspection may have been rejected; now allowed.
3. **Device registration triggers** (pairing) and **device deletion started/completed triggers** (RM) — trigger event types.
4. **FDO authentication** (experimental, disabled by default) — a new pairing auth path.
5. **Realm-scoped health** `GET /pairing/v1/{realm}/health` — upstream 1.3 now has this; Astrate's deviation 18 note saying "which upstream 404s" is now against 1.2.x.
6. Many AMQP/container/config changes that are NOT wire-surface-relevant to a wire-compatible client.

But COMPATIBILITY.md is fundamentally a document about Astrate's target version (1.2.2) and its deviations. The proposal I write should reflect what a v1.3.2-aware update would say, and flag it as a decision for Giulio.

However, I must be careful per MULE.md: "Never invent an API, a config key, or a behaviour. Read the code or run it." and "Never invent a docs page wording that carries the frozen spec." The task is to propose wording. This is a proposal for Giulio's decision, so it needs to be accurate and evidence-based.

Let me not overreach. The task title is `compat-note-v1.3.2` and says "propose the docs/COMPATIBILITY.md wording update for upstream v1.3.2 (latest stable; v1.4.0 is still rc-only) in .mule/for-giulio.md". 

The cleanest, most accurate proposal is a for-giulio entry that:
- Notes upstream v1.3.2 is now the latest stable (v1.4.0 rc-only), while COMPATIBILITY.md is written against 1.2.2 (APICompatVersion 1.2.2, internal/realm/service.go:588).
- Summarizes the v1.3.0 wire-relevant changes (capabilities MQTT feature incl. `purge_properties_compression_format`, empty-introspection allowance, device registration + deletion started/completed triggers, FDO experimental auth, realm-scoped health now upstream-native).
- Flags the specific doc passages that would become inaccurate (deviation 18's "which upstream 404s" claim about the realm health path now shipped in 1.3; deviation 1's zlib purge description).
- Proposes the wording, framed as: the doc should state Astrate's target remains 1.2.2, note that v1.3.x is not yet emulated, and list what a v1.3.2 reconciliation would need before bumping.
- Asks whether v1.3.2 should become the new target (decision).

Let me check the deviation-18 text and deviation-2 wording about the health route, and confirm the APICompatVersion bump rule. The bump rule in service.go says bump only in the same change that completes the full surface of the new level. So a v1.3.2 target is not reached by a doc wording flap; it's a milestone decision.

Let me verify the process (from issue slugs): `compat-note-v1.3.2` is not `issue-<N>` or `milestone-...`, it's an auto task. So it's proposing. Good.

Let me now check whether there's a notable nuance: COMPATIBILITY.md deviation 18 currently says upstream 404s the realm-scoped health route. Now Astrate's pairing/http.go:78 comment already says "upstream 1.3+". So the code already knows. The doc just hasn't been updated. My proposal wording should reflect that Astrate already serves the route that upstream 1.3 added.

Let me also double check that COMPATIBILITY.md title/target line needs updating. Actually, the doc doesn't have a single "targets upstream X.Y.Z" line at top; it references many 1.2.0/1.2.2 measurements. The proposal should be conservative: propose a short note, not a rewrite.

Now let me write the for-giulio entry. Since this is a decision/proposal, I'll add it at the top (newest at top).

Let me read the pairing http.go health comment for accuracy.
→ Read internal/pairing/http.go [limit=20, offset=75]
Now I have the full picture. Let me write the proposal to `.mule/for-giulio.md`, newest at the top. The deliverable is the proposed wording for `docs/COMPATIBILITY.md`, which I propose (not apply) for Giulio's decision.

Let me draft the entry carefully — it must be accurate, cite evidence, and ask the decision (whether v1.3.2 becomes the target, which drives whether COMPATIBILITY.md gets updated). The COMPATIBILITY.md is on the never-touch list, so I only propose the wording.

Let me insert at the top (line 13, after the `---` at line 11).
→ Read .mule/for-giulio.md [limit=12]

← Edit .mule/for-giulio.md
Index: /root/astrate-mule/.mule/for-giulio.md
===================================================================
--- /root/astrate-mule/.mule/for-giulio.md
+++ /root/astrate-mule/.mule/for-giulio.md
@@ -9,8 +9,13 @@
 line once you have dealt with it — this file is a queue, not a log.
 
 ---
 
+- **COMPATIBILITY.md wording update for upstream v1.3.2 (latest stable, 2026-07-14; v1.4.0 is still rc-only).** Astrate's doc and `APICompatVersion` still target upstream **1.2.2** (`internal/realm/service.go:588`); v1.3.0 (2026-05-06) introduced wire-surface changes Astrate does not yet emulate, so this is a decision — adopt v1.3.2 as the compatibility target (then update the doc + bump `APICompatVersion` together, per the bump rule) or keep 1.2.2 and add a "not yet emulated" note. Wire-relevant v1.3.0 deltas (release notes): **MQTT v1 capabilities** incl. `purge_properties_compression_format` (plaintext vs zlib purge — touches the `emptyCache`/`producer,properties` contract COMPATIBILITY.md deviation 1 documents); **empty introspection now allowed**; **device registration triggers** (pairing) and **device deletion started/completed triggers** (RM — the latter two already exist as Astrate deviation 9 emits both around the synchronous delete); **FDO authentication** (pairing, disabled by default); **realm-scoped health** — upstream v1.3 added `GET /pairing/v1/{realm}/health`, which Astrate already serves (`internal/pairing/http.go:78-81`, comment already says "upstream 1.3+"), so deviation 18's wording ("which upstream 404s") is now false against 1.3 and the note should be reworded either way. Proposed doc wording (for your approval, edit to taste): in §Infrastructure differences add a sentence — *"Compatibility target: upstream **v1.2.2** (`GET /v1/{realm}/version` reports `1.2.2`). Upstream v1.3.x capabilities (MQTT v1 capabilities incl.
+
+plaintext `purge_properties_compression_format`, empty-introspection allowance, device registration/deletion triggers, experimental FDO pairing auth) are not yet emulated and are out of scope until the milestone that adopts v1.3.2 as the target."* — and reword deviation 18's realm-health note from "which upstream 404s" to "added by upstream v1.3 (Astrate serves it against a 1.2.2 target; kept, matching behavior)". Raw upstream changes: [v1.3.0](https://github.com/astarte-platform/astarte/releases/tag/v1.3.0), [v1.3.2](https://github.com/astarte-platform/astarte/releases/tag/v1.3.2).
+
+---
 - **The mule's 42 straight failures were ten lint errors in its own base, not a stale branch.**
   `mule/queue` stopped taking `main` on 2026-07-27 and drifted 120 commits behind, which is
   real and is why it is being rebuilt — but it is not what blocked the work. The lint gate runs
   `golangci-lint run ./...` over the whole repo, and the branch's own `internal/flow/*` code

I've added the proposal to `.mule/for-giulio.md`. The task is complete — no code change, no git touched.

Done: Proposed docs/COMPATIBILITY.md wording for upstream v1.3.2 in .mule/for-giulio.md (decision needed: adopt v1.3.2 as target or keep 1.2.2).
Files: .mule/for-giulio.md
Verified: read docs/COMPATIBILITY.md, internal/realm/service.go:588, internal/pairing/http.go:78-81; fetched upstream v1.3.0/v1.3.1/v1.3.2 release notes -> n/a (no code change)
Unsure: Whether v1.3.2 should become the compatibility target (real decision, left to Giulio); the exact wording is my proposal, not applied
Follow-ups: none
