---
name: project-astrate-phases
description: Astrate project goal, hard constraints, and the strict 3-phase workflow (Phases 1-2 done; Phase 3 codegen needs explicit user approval)
metadata:
  type: project
---

Astrate is a lean Go re-implementation (spiritual fork) of the Astarte IoT platform, developed in `/Users/atsetilam/astrate`.

**Why:** Strip Astarte's operational complexity (Kubernetes, Cassandra/ScyllaDB RAM footprint, fragmented Elixir microservices) so it runs on a 1-2 GB VPS or at the edge, while staying wire-compatible with official Astarte device SDKs.

**Hard constraints (from the user's master prompt — do not relax):**
- Go only, single statically linked binary, modular monolith (no microservices)
- PostgreSQL + TimescaleDB only (no Cassandra); embedded MQTT broker (mochi-mqtt) preferred
- docker-compose or bare binary deployment, zero Kubernetes
- SDK compatibility is CRITICAL: exact MQTT topics (`<realm>/<device_id>/...`), Astarte MQTT v1 protocol, pairing REST API, mTLS cert CN `<realm>/<device_id>`, JWT claim model
- Dual payloads: BSON (official SDKs) AND plain JSON (for AtomVM / constrained devices)

**Workflow state:** Strict phase gating — never skip ahead without explicit user approval.
- Phase 1 (architecture design doc): DONE 2026-06-10 → `docs/DESIGN.md`. APPROVED as-is by user, no amendments.
- Phase 2 (roadmap): DONE 2026-06-10 → `docs/ROADMAP.md`. APPROVED by user 2026-06-10. 10 milestones M0-M9, 12 codegen sessions (S1-S12), 4 SDK conformance checkpoints CP-A (after M4 pairing), CP-B (after M6 engine), CP-C (after M7 APIs), CP-D (M9 full matrix). Test tiers T1 unit / T2 testcontainers-TimescaleDB / T3 E2E / T4 official-SDK conformance / T5 load+security.
- Phase 3 (code generation): IN PROGRESS, one session per milestone, each ends with its gate green + a commit on `main`. ROADMAP.md §0.1 fixes binding generation rules (strict dependency order, tests ride with subject, no TODOs except 3 named extension points) and §11 maps milestones to sessions — follow it literally.
  - S1/M0 DONE 2026-06-11, commit 4bbf698: scaffolding, dep pinning, testutil harness, CI skeleton. Gate green locally (lint 0 issues, T1 green, T2 container test boots latest-pg16, timescaledb ext 2.27.2).
  - S2/M1.1-M1.3 DONE 2026-06-11 (committed on main right after 4bbf698): `pkg/deviceid` (astartectl-parity vectors, FuzzParse), `pkg/astarteapi` (frozen golden envelopes incl. Phoenix-default detail strings "Not Found"/"Bad Request" + Astarte-specific "Device not found"; DecodeData unwrapper), `pkg/interfaceschema` (strict parser, zero-alloc backtracking EndpointTrie, Compile→§2.6 structs, CheckMinorUpgrade). Gate green: lint 0 issues, T1+race, both fuzz targets 10s, TestMatchZeroAllocs==0.
  - S3/M1.4 DONE 2026-06-11, commit d9df3f4: `pkg/payload` dual codec complete (value/sniff/bson/json/payload + 32 golden vectors + fuzz). Gate green: lint 0, T1+race, FuzzDecode 10s ~800k execs, caps enforced both formats. M1 fully closed.
  - S4/M2 DONE 2026-06-11, commit 7616550: migrations 000001-000005 (DESIGN §2.2/§2.3 byte-identical; §2.4 adds only the object_datastreams compression statements that the design's own "same shape as above" comment instructs; §2.5 TTL proc also sweeps object_datastreams — flagged to user as an extension beyond the design's literal individual-only DELETE) + full `internal/store` (store/crypto/realms/interfaces/devices/properties/datastreams/groups/triggers/notify). Gate green: lint 0, T1, full make test-integration (~43s store suite, 10k batch in ~0.9s).
  - S5/M3+M4 DONE 2026-06-12, commit 438c998: `internal/auth` (jwt/claims/cache/middleware) + `internal/pairing` (+ca, ratelimit, http) + CP-A green (official astarte-device-sdk-go v0.90.2 pairing flow + astartectl v26.5.0 register against live composed instance). Gate green: lint 0, T1+race, full make test-integration, make test-conformance.
  - S6/M5 DONE 2026-06-12: `internal/broker` complete (intake/identity/authhook/aclhook/sessionstore/lifecycle/publisher/broker + testutil/mqttclient.go). Gate green locally: lint 0, T1+race, full `make test-e2e` (broker e2e 14 subtests ~84s incl. restart persistence, ordered QoS1 replay, QoS2 both directions, 1s-expiry drop across restart, deferred ack, takeover). COMMIT BLOCKED at session end: gpg signing failed ("No passphrase given" — agent cache empty, no pinentry from the harness; S1-S5 signed fine). Work staged; user must unlock the key or authorize --no-gpg-sign. Verify commit landed before starting S7.
  - S6/M5 commit DID land (3d2bb72) — the gpg block was resolved; M6a/M6b committed unsigned/fine since.
  - S7/M6a DONE 2026-06-14, commit ca3ada7: `internal/engine` pipeline + data path (cache.go schema/device caches behind atomic.Pointer + LISTEN/NOTIFY invalidation, router.go sharded Submit/Drain implementing broker.Intake+LifecycleSink, topics.go classify, data.go §2.6 validation pipeline → PersistOp, batch.go per-shard micro-batch ack-after-commit + DB-outage parking, + tests). Gate green: lint 0, T1+race, T2 engine integration (typed columns, object rows, properties, ack-after-commit, both invalidation paths).
  - S8/M6b DONE 2026-06-14, commit 72d2d92: introspection.go, control.go (emptyCache resync + producer/properties zlib purge + consumer/properties builder; zip-bomb caps), serverdata.go (PublishServerValue/UnsetServerProperty), engine.go (New/AttachBroker/Start/Drain/Bus/RefreshTriggers wiring), triggers/{match,events,actions}.go, stream/bus.go + all tests; plus new T1 bus_test.go & actions_test.go, the reusable testutil/astartedevice.go (item 6.15), engine_e2e_test.go (T3), and CP-B at test/conformance/cpb. Gate: lint 0, T1+race, T2, CP-B (6/6, official astarte-device-sdk-go full loop), T3 broker-e2e (race, 14/14, ~39s) and engine-e2e (race, 5/5, ~20s) ALL GREEN — full M6 gate met. (Broker e2e initially could not run because this host's Docker Desktop was CPU-saturated / the daemon went down; it passed cleanly after restarting Docker. The commit message 72d2d92 still notes the original caveat as a point-in-time record.)
  - S9/M7a DONE 2026-06-14, commit dfe6dfd: `internal/realm` (service.go + http.go: interface install/list/get/minor-update[CheckMinorUpgrade]/delete[store draining rules], trigger CRUD via triggers.Compile, config/auth key rotation; NOTIFY + engine RefreshInterfaces/RefreshTriggers on every mutation) + `internal/housekeeping` (service.go + http.go: realm create = ca.Generate + sealer.Seal + store.CreateRealm tx, get/list/delete-cascade). Gate: T2 (race) both suites GREEN (realm 10/10, housekeeping 7/7 incl. cross-domain created-realm-serves-pairing), lint 0. M7 NOT fully closed — M7b + CP-C remain.
  - S10/M7b IMPL+T2 DONE 2026-06-14, commit 00a641f: `internal/appengine` (service.go devices+groups, data.go queries/§2.3-re-encode/server-writes, http.go /appengine/v1/{realm} a_aea) + `internal/appengine/stream/ws.go` (/astrate/v1/{realm}/socket WebSocket[coder/websocket]+SSE off engine.Bus, a_ch). Verified: T2 appengine suite (race 8/8: auth, device status, datastream since-incl/since_after-excl/limit/descending-default, longinteger>2^53→string, property tree, server write via fake, PATCH, groups) + SSE socket T1 test (race) + lint 0. REMAINING for the M7 gate: T3 cross-domain + CP-C (astartectl) — NOT yet done.
  - T3 cross-domain DONE 2026-06-14, commit 631ac86: internal/appengine/e2e_test.go (//go:build integration && e2e) composes store+engine+broker+pairing+AppEngine (CP-B style) — ServerOwnedPutReachesDevice (AppEngine PublishData→engine→broker→real device) + InhibitBlocksReconnect (AppEngine PATCH inhibit ⇒ broker rejects CONNECT + pairing refuses credentials). Race-clean against ASTRATE_TEST_DSN (the testcontainer readiness probe times out under this host's Docker load — use `make up` + ASTRATE_TEST_DSN).
  - S10/M7b CP-C DONE 2026-06-14, commit e959445 (M7 GATE FULLY MET): test/conformance/cpc (//go:build integration && e2e) composes store+engine+broker+pairing+ALL M7 HTTP APIs+stream socket on one httptest mux; drives the pinned astartectl v26.5.0 through: housekeeping realms create → broker.ReloadRealms (harness calls it explicitly; M8 must wire housekeeping-create→broker.ReloadRealms) → realm-management interfaces install genericsensors.Values → pairing agent register → testutil.AstarteDevice publishes /sensor0/value=42.5 (in-process Credentials from astartectl's secret; CSR path already locked by CP-A/B) → astartectl appengine devices data-snapshot/get-samples/list → triggers install/list/show/delete. astartectl helper copied into package cpc (astartectl_test.go: ensureAstartectl/runAstartectl + tryAstartectl for the failure path). astartectl flags: `-u <httptestURL>` derives <url>/{housekeeping,realmmanagement,appengine,pairing}; housekeeping `--housekeeping-key instancePriv.pem --realm-public-key realmPub.pem -y`; realm/appengine/pairing `--realm-key realmPriv.pem -r <realm>`. Keys: RSA-2048 PKCS1 priv + PKIX pub PEM files (astartectl mints RS256 JWTs with the right claim per command). Run: `make up`/ASTRATE_TEST_DSN + cached astartectl (skips otherwise); no CGO needed (cpc doesn't import the SDK).
    - CP-C wire-shape outcome: only ONE fix was forced — AppEngine interface-ROOT queries (no path) must return the NESTED upstream snapshot tree, NOT the old flat array/map (astarte-go nestedIndividualQuery unmarshals the body into map[string]interface{} then walks it via parseDatastreamMap[leaf key "value"]/parsePropertiesMap; a flat array fails the decode outright). Implemented in appengine/data.go: individual-datastream root → {seg:{...:{value,timestamp}}} (latest sample/endpoint via new store.IndividualSnapshot = DISTINCT ON (path) ORDER BY path, ts DESC); properties root → nested value tree (was flat {path:value}). CONCRETE-path queries UNCHANGED (datastream → [{value,timestamp},…] array for get-samples; property → single value). nestTree(map[path]any) splits on "/". data-snapshot for a parametric individual datastream does realm-management interface-type resolution first (GET interface def), so realm-mgmt + appengine are exercised together. Everything else I had CHOSEN held: device-list single page (astarte-go reads links.next in the BODY not my X-Astrate-Next-Token header — only matters for multi-page, untested here, so the header is still a latent deviation if astartectl ever paginates >1 page); sort default descending; PUT/POST 200 / DELETE 204; groups body. Empty datastream snapshot returns {} which astarte-go's parseDatastreamMap treats as malformed → ensure data exists before data-snapshot (CP-C publishes first).
    - BROKER ROBUSTNESS FIX shipped in e959445 (drift caught composing CP-C against the shared compose DB, which appengine T2 pollutes with fake CACertificatePEM:"ca" realms): realmPools.Reload now SKIPS a realm whose stored CA fails ca.ParseCertificatePEM (log.Warn + continue) instead of returning the error and refusing to start the whole broker (DESIGN §6 single-process blast-radius). newRealmPools now takes the broker logger. Devices in a skipped realm just fail the handshake until re-keyed. Production realms are CA-valid by construction (housekeeping ProvisionCA), so this only bites on a polluted/corrupt DB. No broker test asserted the old hard-fail.
  - S11/M8 is next (config/binary/packaging): see the "HTTP wiring for M8" facts below — all five APIs (pairing/realm/housekeeping/appengine/appengine-stream) mount on ONE ServeMux in cmd/astrate. M8 must also wire housekeeping realm-create (and realm re-key) to call broker.ReloadRealms(ctx) so a freshly-created realm's CA is trusted without a restart (CP-C's harness does this manually). engine.New→broker.New(intake=sink=engine)→AttachBroker(AdaptBroker(b))→engine.Start→broker.Start; shutdown broker.Close FIRST then engine.Drain.
  - S10/M7b facts (the close-out + M8 need):
    - HTTP wiring for M8: `appengine.NewAPI(appengine.NewService(st, engine, log), mw).Mount(mux)` (a_aea; engine satisfies appengine.ServerData via PublishServerValue/UnsetServerProperty — may be nil for read-only); `apstream.NewAPI(engine.Bus(), mw).Mount(mux)` (a_ch; engine.Bus() is *engine/stream.Bus, satisfies the apstream.Bus interface). All five APIs (pairing/realm/housekeeping/appengine/appengine-stream) mount on one ServeMux in cmd/astrate.
    - T3/CP-C compose pattern = CP-B's (test/conformance/cpb): in-process store+engine+broker+pairing+the M7 HTTP APIs on an httptest server; broker binds 127.0.0.1:0 then pairing.Config.BrokerURL=mqtts://+broker.TLSAddr(); drive with testutil.AstarteDevice (item 6.15) and/or the official SDK / astartectl. CP-C lives in the conformance module (separate go.mod, CGO; `go -C test/conformance mod tidy` after adding appengine/realm/housekeeping imports). astartectl pinned binary cached at ~/Library/Caches/astrate-conformance/astartectl-v26.5.0 (see test/conformance/cpa/astartectl_test.go ensureAstartectl/runAstartectl helpers — reuse them; they're package cpa so copy or factor a shared helper).
    - M7b wire shapes CHOSEN, NOT astartectl-validated (CP-C validates; fix there if it disagrees): device-list page via ?limit + ?from_token, next cursor in X-Astrate-Next-Token header + data=[ids]; datastream sample {value,timestamp}; datastream default sort DESCENDING (sort=ascending to flip); downsample_to currently a *duration in QueryOpts but NOT wired into http.go query parsing (upstream downsample_to is a POINT COUNT — needs range→bucket conversion; wire it at CP-C if astartectl uses it); property root = flat {path:value} map (NOT a nested tree — upstream may want nesting); PUT/POST server data → 200, DELETE → 204; groups POST body {group_name,devices[]} requires ≥1 device. Value re-encode (data.go individualValue): longinteger→strconv decimal string, binaryblob→base64.Std, datetime→"2006-01-02T15:04:05.000Z", arrays→stored jsonb passthrough.
    - Env: this dev host's Docker Desktop is flaky/CPU-saturated and the daemon sometimes goes down — restart with `open -a Docker` then poll `docker info`; e2e/T2 testcontainers need it. `make up` (compose DB on :5432) + ASTRATE_TEST_DSN avoids per-package container boots for faster local T2.
  - S9/M7a facts S10+ should know:
    - Wiring for M8: `realm.NewService(st, inv, log)` where inv is the *engine.Engine (satisfies realm.Invalidator: RefreshInterfaces/RefreshTriggers; may be nil) + `realm.NewAPI(svc, mw).Mount(mux)` (a_rma). `housekeeping.NewService(st, sealer, log)` + `housekeeping.NewAPI(svc, mw, instanceKeysPEM).Mount(mux)` (a_ha STATIC instance keys — config-provided in M8; auth base is "v1" not realm).
    - M7b appengine consumes: engine.PublishServerValue / UnsetServerProperty (server-owned PUT/POST/DELETE); engine.Bus() → stream.Bus.Subscribe(realm, stream.Filter, buf) for the socket; store.Series/ObjectSeries/Downsample + ListProperties (GET data, re-encode per stored value_type: longinteger→decimal string, binaryblob→base64, datetime→RFC3339, §2.3); store device APIs (list paged/get/SetDeviceInhibited/aliases+attributes patch/by-alias) + groups APIs. PATCH credentials_inhibited=true must make broker reject next CONNECT + pairing reject credentials (T3 cross-domain).
    - HTTP/test pattern reused from realm/housekeeping: mirror pairing/http.go (astarteapi.WriteData/WriteError/WriteBadRequest/WriteNotFound/DecodeData unwraps {"data":...}); routes via mux.Handle("METHOD /.../{realm}/...", require(HandlerFunc)). T2 tests mint JWTs inline: jwt.NewWithClaims(RS256, jwt.MapClaims{"a_aea":[]string{".*::.*"}, "exp":...}).SignedString(rsaPriv); realm JWTPublicKeysPEM=[pubPKIXpem]; httptest.NewRecorder + mux.ServeHTTP; the realm/housekeeping test helpers (mintToken/pubPEM/decodeData/contains/jsonStr) are per-package _test (copy or factor a shared testutil if wanted). A config/auth-style key-rotation test must keep the original key in the set (concat) so the test token keeps verifying.
    - Wire shapes CHOSEN in M7a but NOT yet astartectl-validated (CP-C at M7b end is the validator — adjust there if astartectl disagrees): realm-mgmt validation 422 = WriteError(detail) (not the Phoenix changeset field-map); dup interface/trigger 409; delete major!=0 → 422 "Interface major version is not 0…"; delete introspected → 422 "Cannot delete an interface that is used by a device introspection"; housekeeping realm body = {realm_name, jwt_public_key_pem, device_registration_limit} (Cassandra replication fields omitted); config/auth field = single jwt_public_key_pem string (may concat multiple PEM blocks; stored as JWTPublicKeysPEM []string{that}).
    - store-side facts relied on: DeleteInterface already enforces draining (ErrInterfaceMajorNotZero, ErrInterfaceInUse); UpdateInterface enforces additive endpoints but NOT full CheckMinorUpgrade (the realm service runs CheckMinorUpgrade(prev,next) first). store.GetRealmByName/ListRealms/SetRealmJWTPublicKeys/CreateRealm(ErrInvalidRealmName/ErrAlreadyExists)/DeleteRealm(cascade).
  - S8 implementation facts S9+ should know:
    - Engine M8/M7 wiring contract (engine.go): `New(st Store, bp BrokerPort, cfg Config)` — bp may be nil at construction; `AttachBroker(AdaptBroker(broker))` before `Start(ctx)`. Order: engine.New → broker.New(ctx,cfg,st,intake=engine,sink=engine) → AttachBroker → engine.Start(ctx) → broker.Start(). Shutdown: broker.Close() FIRST (so no Submit blocks) → engine.Drain(ctx). `engine.RefreshInterfaces(ctx, realmID)` (M6a) and `engine.RefreshTriggers(ctx, realmID)` are the in-process invalidation callbacks M7 realm CRUD must call after store mutations (triggers ride in the same realm snapshot as interfaces); store.NotifyInterfacesChanged is the cross-process path.
    - AppEngine (M7b) server-data API on *Engine: `PublishServerValue(ctx, realm, id, ifaceName, path, value json.RawMessage, ts *time.Time) error` (PUT/POST), `UnsetServerProperty(ctx, realm, id, ifaceName, path) error` (DELETE). Map sentinels to HTTP: ErrRealmUnknown/ErrInterfaceNotFound/ErrPathNotFound→404, ErrNotServerOwned/ErrNotAProperty/ErrUnsetNotAllowed→422/4xx; payload validation returns *payload.RejectError. `engine.Bus()` (stream.Bus) feeds the M7b WebSocket/SSE socket: Subscribe(realm, stream.Filter{DeviceID,Interface}, buffer) → (<-chan Event, cancel); non-blocking, slow-consumer drop + metric.
    - testutil.AstarteDevice (item 6.15, reusable for M7/M9): ConnectAstarteDevice(t, sslURL, realm, id, tlsCfg, cleanSession) subscribes base/# and captures server msgs; PublishIntrospection, PublishValue(iface,path,Value,ts,Format,qos), PublishRaw (empty body=unset), EmptyCache, SendProducerProperties, WaitForMessage/WaitForTopic, Disconnect; helpers testutil.Introspection(map[string][2]int), Deflate/InflateControlList. Does NOT import internal/broker (avoids the cycle: broker tests import testutil).
    - CP-B harness (test/conformance/cpb): mirrors cpa (separate module, replace ../.., CGO needed — SDK pulls bson/sqlite); composes engine+broker+pairing in-process; broker binds 127.0.0.1:0 then pairing.Config.BrokerURL="mqtts://"+broker.TLSAddr() (SDK rewrites mqtts→ssl, IgnoreSSLErrors=true skips the self-signed server cert). SDK device: NewDeviceWithOptions + AddInterface(every iface) + OnIndividualMessageReceived/OnAggregateMessageReceived callbacks; MkdirAll the CryptoDir. `go -C test/conformance mod tidy` after adding internal/broker+engine imports. Run needs a DB: `make up` (compose on :5432) or ASTRATE_TEST_DSN.
    - BROKER ACL FIX shipped in 72d2d92 (drift caught by CP-B): the official SDK connect order is subscribe→introspection→emptyCache, so it subscribes to base/<server-iface>/# BEFORE its introspection exists. checkACL now allows ANY subscribe FILTER (topic containing +/#) within the device's own base subtree; concrete-topic DELIVERY stays gated on introspected server ownership (wildcards never appear in delivery topics, so the two are told apart by that). DESIGN §3.2 SUBSCRIBE row clarified to match. aclhook_test.go matrix updated.
    - Trigger executor (triggers/actions.go): Forwarder is the §1.1 extension point (nil = custom/AMQP actions counted "skipped", the designed v1 default). Webhook: 2xx delivered, 4xx permanent-fail, 5xx/transport retried with exp backoff; full bounded queue drops (never backpressures ingestion). Match/event JSON frozen to upstream SimpleEvents shape; value_change*/path_*/group-scoped accepted-but-not-evaluated (Trigger.Unsupported).
    - Env reality on this dev host: Docker Desktop ~7.75 GB and frequently CPU-saturated → e2e testcontainers (StartTimescale) time out / won't provision. For local T2/T3, prefer `make up` + ASTRATE_TEST_DSN to reuse the compose DB (StartTimescale honors it); but DON'T share ASTRATE_TEST_DSN into the store pkg's destructive MigrationCycle run. broker/engine e2e use per-run-unique realms so sharing one DB between them is safe.
  - S6 implementation notes S7+ should know:
    - Broker API for the engine: `broker.New(ctx, Config, Store, Intake, LifecycleSink)`, `Start/Close`, `Publisher().Publish(topic, payload, qos, retain, expiry)`, `RefreshIntrospection(ctx, realm, deviceID)` (engine MUST call after persisting new introspection — broker caches iface→ownership per session for ACLs, with a 1s-debounced self-heal reload on ACL miss), `ReloadRealms(ctx)` (M7 realm CRUD). `InboundMessage{Realm, DeviceID, Topic, Payload, QoS, ReceivedAt, Ack}` — Ack is idempotent, never nil; QoS≥1 blocks that device's read loop inside OnPublish until Ack() (mochi writes PUBACK after OnPublish returns — that IS the deferred-ack/backpressure mechanism). batch.go must Ack only after commit.
    - mochi v2.7.9 facts baked in: return `packets.CodeSuccessIgnore` from OnPublish = ack-but-no-fanout/no-retain (device publishes are consumed; no echo to B/# subscribers); inline publisher client pinned to ProtocolVersion 5 because the inflight-expiry sweeper only honours pk.Expiry on PV5 packets; mochi sorts inflight replay by uint16(Created) (whole seconds — collides), so sessionstore persists a Seq per message and rewrites Created strictly-increasing at restore; storage.Message can't carry Expiry/PV back into the server, so expiry across restarts = load-time filtering + OnSessionEstablish pruning of the inherited queue; ACL read checks run at DELIVERY time too → `offlineACL` store-fallback lets server publishes queue for disconnected sessions (engine offline publishes depend on it).
    - Wills are stripped in OnConnect (mochi publishes LWTs without a write-ACL check — would be a planted-retained-message hole).
    - Broker server TLS identity is `Config.ServerTLSCert` (tests: `testutil.ServerTLSCert(t)` self-signed + client root pool). M8 open question: SDKs trust pairing's ca_crt as root, so the real deployment needs the broker cert issued from a realm CA (needs serverAuth EKU — ca.SignCSR only does clientAuth) or a fleet-trusted CA.
    - Test plumbing: `testutil.MQTTConnect/MQTTTryConnect(t, url, clientID, cleanSession, tlsCfg, ...tweaks)` (paho, returns session-present), `DeviceCSR`, `DeviceTLSConfig`, `WaitToken`. Recorders (`recorderIntake` manual/auto-ack, `recorderSink`) + `waitFor` + `discardLogger` live in plain (untagged) broker _test files so the e2e-tagged suite reuses them. e2e umbrella TestBrokerE2E boots one container; per-subtest brokers on 127.0.0.1:0 with per-subtest bbolt paths in t.TempDir().
    - revive enforces context-as-second-arg even in tests (`func testX(t *testing.T, ctx context.Context, ...)` flags) — create ctx inside helpers instead.
  - S5 implementation notes S6+ should know:
    - Upstream parity facts dug out of astarte v1.2.0 source (frozen into goldens): authz strings split on ":" with SplitN parts=3 — middle "opts" field IGNORED ("a::b" is the empty-opts case); anchoring is literal "^"+s+"$" concatenation (alternation escapes anchors — tested quirk); auth_path = segments after first segment == realm. Missing/invalid token→401, valid-token claim-mismatch→403. Pairing 422s use Phoenix changeset shape incl. literal "error_name" field ({"errors":{"error_name":["already_registered"|"device_registration_limit_reached"]}}); bad hw_id msg "is not a valid base64 encoded 128 bits id"; verify timestamps render as Elixir DateTime.to_string → Go layout "2006-01-02 15:04:05.000Z"; invalid-verify body carries "details":null; info statuses pending/confirmed/inhibited (inhibited may still GET info). DELIBERATE deviation (ROADMAP-frozen): wrong secret/unknown device → uniform 401 (upstream returns 403 via its RPC "forbidden" mapping).
    - Broker hookups ready: device CN is "<realm>/<deviceID.String()>"; devices.cert_serial = cert.SerialNumber.String() (decimal), cert_aki = lowercase hex of CA SKI; realm CA PEM at store.Realm.CACertificatePEM (per-realm client-CA pools from there); ca.ParseCertificatePEM exported; issue test client certs via pairing.Service.Credentials or ca.SignCSR. auth exports: NewMiddleware(st).RequireRealm/RequireStatic, TokenFromContext, RelativePath (revive rejected the name AuthPath as stuttering).
    - pairing API: pairing.New(st, sealer, Config{BrokerURL, CertTTL, EnforceLatestCert, Version, BcryptCost}), NewAPI(svc, mw, APIConfig{rate limits}).Mount(mux); ProvisionCA(realm, lifetime, sealer) → (certPEM, sealedKey) for housekeeping (M7).
    - test/conformance is a separate go.mod (replace ../..): internal/* imports legal because module path shares the astrate/ prefix; builds need CGO (SDK pulls gorm/sqlite). SDK fast-fail recipe: NewDeviceWithOptions with UseMqttStore/UseDatabase=false, CryptoDir set, ConnectRetry=false + AddInterface(≥1) then Connect — pairing completes, MQTT fails fast on closed port. astartectl pinned binary cached at ~/Library/Caches/astrate-conformance/astartectl-v26.5.0 (use --config-dir tmp to silence config warnings, --compact-output prints bare secret). dialDSN: ASTRATE_TEST_DSN else compose DSN else skip — run `make up` before make test-conformance.
    - Don't share one DB (ASTRATE_TEST_DSN) across a full `make test-integration` run: store's destructive MigrationCycle subtest races other packages' suites; container-per-package is the safe default.
    - Go x509 fact: a root CA cert presented as a leaf verifies against itself (absent EKU = any); don't assert otherwise (upstream pkix_path_validation matches).
  - S4 implementation notes S5+ should know:
    - store API surface S5 needs: `store.New(ctx,dsn)` self-migrates; `KeySealer` (Seal/Open, `NewKeySealerFromEnv`, ASTRATE_MASTER_KEY[_FILE]) is standalone — realms repo stores/returns SEALED bytes only (`Realm.CAPrivateKeySealed`), pairing/ca layer seals+opens. Device flow methods: RegisterDevice (rotate-until-confirmed, ErrDeviceAlreadyConfirmed), UnregisterDevice (clears trail, keeps row), SetDeviceCredentials(serial,aki,ip→status confirmed), SetDeviceInhibited, CountDevices (for device_registration_limit).
    - migrations live top-level with `migrations/migrations.go` (go:embed needs an in-package Go file); migrate runner uses stdlib.OpenDBFromPool (its Close does NOT close the pgxpool) + database/pgx/v5 WithInstance.
    - pgx facts relied on: zero-arg Exec always uses simple protocol (multi-statement migrations work; `CALL astrate_apply_endpoint_ttl()` may COMMIT internally only when invoked with zero args); uuid passed explicitly as pgtype.UUID{Bytes:..,Valid:true} everywhere (don't rely on named-[16]byte wrapping); jsonb params/scans go through encoding/json automatically (maps/[]string fine); nullable inet/timestamptz scan into **netip.Addr/**time.Time via pointer fields.
    - test pattern: one umbrella TestStore boots a single container; per-file `testXxx(t,s)` subtests; per-run-unique realm names keep ASTRATE_TEST_DSN reruns green; MigrationCycle (down-to-1/up) is destructive and must stay the LAST subtest. gosec needed `#nosec G304 G703` (taint rule separate from G304) and `#nosec G118` for the listener goroutine; revive rejects Min/Max suffixes on time.Duration consts (reads them as minutes).
    - Series `to` bound implemented INCLUSIVE (<=) — upstream Astarte semantics for `to` unverified; recheck at M7/CP-C when wiring appengine queries.
  - S3 implementation notes S4+ should know:
    - golangci-lint sits at /Users/atsetilam/go/bin/golangci-lint; GOPATH/bin is NOT on PATH, so `make lint` fails — invoke the binary by absolute path (or export PATH first).
    - pkg/payload public API: `Decoder{MaxSize}`, `Decode(p, *CompiledMapping)`, `DecodeObject(p, leaves map[string]*CompiledMapping)`, `Encode(v, *time.Time, Format)`, `DetectFormat`, `RejectReason`/`ReasonOf` (snake_case metric labels via `RejectReasons()`), `DecodedPayload{Value,Timestamp,Format}`. Engine should key payload_format_hint off DecodedPayload.Format.
    - Golden BSON vectors live in pkg/payload/testdata/bson/*.hex, captured by `tools/bsoncapture` (standalone Go module pinning astarte-device-sdk-go v0.90.2 deps: astarte-go v0.90.4 + mongo-driver v1.8.0). SDK marshals a Go map, so captured v/t element order is randomized — decoders must stay lookup-based; never byte-compare whole docs against SDK output.
    - stdlib gotchas hit in S3: json.Unmarshal accepts a QUOTED number into json.Number (guard token's first byte); JSON null unmarshals as a silent no-op into bool/string/Number (explicit isJSONNull checks everywhere); mongo bson.Raw.Validate tolerates trailing bytes (sniff prefix==len check is what excludes them).
  - S2 implementation notes S3+ should know: upstream standard-interfaces live in astarte-platform/astarte repo under `standard-interfaces/` (NOT a separate repo); vendored at commit d6a1f5e578 into pkg/interfaceschema/testdata/valid/ with provenance in testdata/README.md. CompiledMapping lives in trie.go (not compile.go) so trie tests compile standalone. EndpointIDResolver is a 2-method interface (ResolveInterface/ResolveEndpoint), nil → zero IDs. gosec #nosec needs to sit on the flagged line itself, not above the import block.

**Phase 3 implementation facts S2+ must know (not derivable at a glance):**
- Module path `github.com/astrate-platform/astrate` (taken from the git remote).
- Do NOT run `go mod tidy` until all pinned deps are actually imported — go.mod hand-pins all §1.1 deps in a direct require block before code imports them; tidy would silently drop the unused ones. If a build hits "missing go.sum entry", `go get <pkg>@pinned-version` for that subpackage instead (this is how jackc/puddle was added for pgxpool).
- golangci-lint pinned at v2.12.2 (v2 config schema), installed via `make tools`; lint runs with `integration`+`e2e` build tags so tag-gated suites are linted. revive's exported/package-comment rules are active — every package needs a package comment and exported symbols need `Name ...`-style doc comments.
- testutil conventions: `testutil.StartTimescale(t)` (T2, honors ASTRATE_TEST_DSN reuse), `testutil.Golden(t, name, got)` with the `-update` flag registered in package testutil — other packages must not re-register an `-update` flag.
- Build tags: T2 = `//go:build integration`, T3 = `e2e` (Makefile runs e2e with both tags).

**Dependency pins decided in ROADMAP.md M0 (frozen unless user amends):** stdlib net/http (Go >= 1.22 ServeMux), mochi-mqtt/server/v2, pgx/v5, golang-migrate/v4 + go:embed, mongo-driver/v2 bson (raw API), golang-jwt/v5, x/crypto/bcrypt, bbolt (chosen over pebble), hashicorp/golang-lru/v2, prometheus client_golang, coder/websocket, BurntSushi/toml, testcontainers-go with timescale/timescaledb:latest-pg16, paho.mqtt.golang (test-only).

**How to apply:** Before starting Phase 3, confirm the user approved Phase 2 (docs/ROADMAP.md). All design decisions (shared hypertable with typed columns, in-process sharded pipeline replacing RabbitMQ, embedded CA replacing CFSSL, payload format sniffing) are frozen in docs/DESIGN.md; the file-by-file order, tests, and dependency pins are frozen in docs/ROADMAP.md — read both before generating code, treat them as the spec unless the user amends them. Schema in DESIGN.md §2.2-2.4 must be transcribed verbatim into migrations; deviations are design-change requests, not ad-hoc fixes.
