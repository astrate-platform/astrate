# Pinned official Astarte Python device SDK for the CP-D pysdk runner
# (docs/ROADMAP.md §0.3, §10 file 9.3). Upgraded deliberately, never by drift.
astarte-device-sdk==0.14.0
# The SDK declares paho-mqtt>=1.3.1 with no upper bound, but 0.14.0 still uses
# the paho 1.x callback API (mqtt.Client() with no CallbackAPIVersion raises on
# paho 2.x) — hold paho back until the SDK pin moves.
paho-mqtt<2
